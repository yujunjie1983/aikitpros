{
  "version": 2,
  "buildCommand": "npm run build",
  "installCommand": "yarn",
  "routes": [
    { "src": "/", "dest": "/landing.html" },
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
